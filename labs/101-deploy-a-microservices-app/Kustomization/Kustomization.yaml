# kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# list of Resource Config to be Applied
resources:
  - redis-enterprise-database.yaml
  - redis-enterprise-database-secret.yaml

# namespace to deploy all Resources to
namespace: default

